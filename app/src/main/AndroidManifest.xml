<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.mybooks">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mybooks"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity_cart"></activity>
        <activity android:name=".activity_book_details" />
        <activity android:name=".activity_books" />
        <activity android:name=".activity_add_books" />
        <activity android:name=".activity_singup" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>